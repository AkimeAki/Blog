---
import Layout from "@/layouts/Layout.astro";
import Post from "@/components/organisms/Post.astro";
import CommentArea from "@/components/molecules/CommentArea.astro";
import { getContentsDetail, getListContents } from "@/lib/microcms";
import type { Blog } from "@/lib/microcms";

interface pathType {
	params: {
		blogId: string;
	};
}

export const getStaticPaths = async (): Promise<pathType[]> => {
	const response = await getListContents<Blog>("blogs", { fields: ["id"] });
	return response.contents.map((content) => ({
		params: {
			blogId: content.id
		}
	}));
};

const { blogId } = Astro.params;
if (blogId === undefined) {
	return;
}

const blog = await getContentsDetail<Blog>("blogs", blogId);
---

<Layout title={blog.title}>
	<Post post={blog} />
	<CommentArea />
</Layout>

<style>
	.comment-note {
		margin-bottom: 10px;
		font-weight: bold;
	}
</style>
