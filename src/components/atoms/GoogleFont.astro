---
const { link } = Astro.props;
---

<script define:vars={{ link }}>
	window.addEventListener("load", () => {
		document.head.insertAdjacentHTML("beforeEnd", `<link rel="stylesheet" href="${link}&display=swap" />`);
	});
</script>
